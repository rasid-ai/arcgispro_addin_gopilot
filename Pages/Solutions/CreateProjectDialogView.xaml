<Window x:Class="Rasid.Pages.Solutions.CreateProjectDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shared="clr-namespace:Rasid.Pages.Shared"
        Title="Create Project"
        Width="1040"
        Height="780"
        MinWidth="850"
        MinHeight="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource RasidBackgroundBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/RASID;component/Resources/SharedResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding SolutionName, StringFormat='Solution: {0}'}"
                   Style="{StaticResource RasidPageTitleStyle}"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="#F7F8F9"
                    BorderBrush="{StaticResource RasidBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    ClipToBounds="True">
                <shared:MarkdownViewer Markdown="{Binding DescriptionHtml}"
                                       VerticalScrollBarVisibility="Auto"
                                       HorizontalScrollBarVisibility="Disabled"
                                       Padding="14"
                                       FontSize="14"
                                       Foreground="#3F4F5F"
                                       Background="#F5F5F5">
                    <shared:MarkdownViewer.Resources>
                        <Style TargetType="{x:Type ScrollBar}">
                            <Setter Property="Width" Value="12"/>
                            <Setter Property="Background" Value="#EEF0F2"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                                        <Grid Background="{TemplateBinding Background}">
                                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                                <Track.DecreaseRepeatButton>
                                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}"
                                                                  Opacity="0" Focusable="False"/>
                                                </Track.DecreaseRepeatButton>
                                                <Track.Thumb>
                                                    <Thumb Margin="3,1">
                                                        <Thumb.Template>
                                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                                <Border x:Name="ThumbBody"
                                                                        Background="#9AA1A6"
                                                                        CornerRadius="3"/>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="ThumbBody" Property="Background" Value="#858D93"/>
                                                                    </Trigger>
                                                                    <Trigger Property="IsDragging" Value="True">
                                                                        <Setter TargetName="ThumbBody" Property="Background" Value="#70787E"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Thumb.Template>
                                                    </Thumb>
                                                </Track.Thumb>
                                                <Track.IncreaseRepeatButton>
                                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}"
                                                                  Opacity="0" Focusable="False"/>
                                                </Track.IncreaseRepeatButton>
                                            </Track>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </shared:MarkdownViewer.Resources>
                </shared:MarkdownViewer>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="18"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Create a Project"
                           Style="{StaticResource RasidPageTitleStyle}"/>

                <StackPanel Grid.Row="1" Margin="0,16,0,0">
                    <TextBlock Text="Project Title:"
                               Style="{StaticResource RasidSectionTitleStyle}"/>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RasidTextBoxStyle}"
                             Padding="13,10"
                             Margin="0,7,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,16,0,0">
                    <TextBlock Text="Tags (separated by spaces):"
                               Style="{StaticResource RasidSectionTitleStyle}"/>
                    <TextBox Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RasidTextBoxStyle}"
                             Padding="13,10"
                             Margin="0,7,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,16,0,0">
                    <Button Content="Cancel"
                            IsCancel="True"
                            Style="{StaticResource RasidSecondaryButtonStyle}"
                            MinWidth="110"
                            Padding="15,10"
                            Margin="0,0,14,0"/>
                    <Button Content="Create"
                            IsDefault="True"
                            Command="{Binding CreateCommand}"
                            Style="{StaticResource RasidPrimaryButtonStyle}"
                            MinWidth="108"
                            Padding="15,10"/>
                </StackPanel>

                <TextBlock Grid.Row="5"
                           Text="Result Preview"
                           Style="{StaticResource RasidPageTitleStyle}"
                           FontSize="14"/>

                <Border Grid.Row="7"
                        Background="#E9EEF1"
                        BorderBrush="{StaticResource RasidBorderBrush}"
                        BorderThickness="1"
                        ClipToBounds="True">
                    <Image Source="{Binding ResultPreview}"
                           Stretch="Uniform"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"/>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
